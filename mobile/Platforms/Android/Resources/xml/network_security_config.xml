<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        PRODUCTION: chỉ cho phép HTTPS.
        Android 9+ (API 28+) block HTTP theo mặc định — giữ nguyên hành vi đó.
        Để test DEV local, thêm lại <domain-config> tạm thời (xem git history).
    -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>

